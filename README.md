# @vpodarke_bot 🎁 — Маркетплейс уникальных подарков

**VPodarke** — это веб-приложение для Telegram (Telegram Web App), которое представляет собой маркетплейс для заказа уникальных творческих подарков и услуг. Платформа объединяет талантливых исполнителей и заказчиков, предоставляя удобный и интуитивно понятный интерфейс прямо внутри Telegram.

Приложение построено как полноценный маркетплейс, где пользователи могут не только заказывать, но и предлагать свои услуги, которые проходят модерацию.

## 🚀 Ключевые возможности

### Для Заказчиков:
-   **Динамический каталог услуг**: Просмотр услуг, созданных исполнителями и одобренных модератором.
-   **Профили исполнителей**: Детальная информация о каждом авторе, включая его биографию и фото.
-   **Простое оформление заказа**: Удобная форма для указания всех пожеланий и загрузки необходимых файлов.
-   **Полная интеграция с Telegram**: Интерфейс адаптируется под цветовую схему Telegram, используются нативные кнопки и уведомления для лучшего пользовательского опыта.
-   **Отслеживание статуса (демо)**: Страница для просмотра истории и статуса своих заказов.

### Для Исполнителей:
-   **Простая регистрация**: Удобная форма для создания профиля и добавления своей первой услуги на модерацию.
-   **Личный кабинет**: Персональный дашборд для отслеживания статуса своих услуг (на рассмотрении, одобрено, отклонено).
-   **Прозрачность**: Исполнитель всегда видит, какие из его услуг активны, а какие требуют доработки.

### Для Администраторов:
-   **Панель модерации**: Специальная страница, доступная только администратору, для управления услугами.
-   **Управление контентом**: Возможность одобрять или отклонять новые услуги, поступающие от исполнителей.
-   **Предпросмотр**: Возможность посмотреть, как будет выглядеть услуга для клиента, перед публикацией.

## 🛠️ Технологический стек

-   **Фреймворк**: React 18+ (с использованием Hooks и функциональных компонентов)
-   **Язык**: TypeScript
-   **Сборка**: Vite
-   **Маршрутизация**: React Router (`react-router-dom`)
-   **Стилизация**: Tailwind CSS (через CDN для быстрой разработки)
-   **Иконки**: Heroicons
-   **Управление состоянием**: React Context API
-   **Интеграция с Telegram**: Telegram Web App API

## 📂 Структура проекта

`
.
├── src/
│   ├── components/       # Переиспользуемые React-компоненты (Header, Layout, ServiceCard...)
│   ├── context/          # Глобальное состояние приложения
│   │   ├── OrderContext.tsx  # Состояние текущего создаваемого заказа
│   │   └── ServiceContext.tsx# Управление списком всех услуг (CRUD, хранение)
│   ├── hooks/            # Пользовательские хуки
│   │   └── useTelegram.ts  # Хук для взаимодействия с Telegram Web App API
│   ├── pages/            # Компоненты страниц (HomePage, ServicePage, AdminPage...)
│   ├── App.tsx           # Основной компонент с маршрутизацией
│   ├── types.ts          # Определения типов TypeScript (Service, OrderDetails)
│   └── index.tsx         # Точка входа в React-приложение
├── index.html            # HTML-файл для входа
├── README.md             # Эта документация
└── metadata.json         # Метаданные для веб-приложения
`

## ⚙️ Техническая реализация и архитектура

### Управление состоянием (State Management)

-   **`ServiceContext`**: Это "мозг" приложения. Он отвечает за хранение и управление всеми услугами.
    -   **Текущая реализация**: Данные персистентно хранятся в `localStorage`. Это идеальное решение для прототипирования и разработки без бэкенда. При загрузке приложения услуги считываются из `localStorage`, а при добавлении или изменении статуса — сохраняются обратно.
    -   **Будущая интеграция**: Вся логика работы с `localStorage` должна быть заменена на асинхронные API-запросы к вашему бэкенду.
-   **`OrderContext`**: Управляет состоянием *одного* создаваемого заказа. Хранит ID услуги, текст пожеланий, загруженный файл и т.д. Эти данные формируют тело запроса при отправке заказа на сервер.

### Интеграция с Telegram (`useTelegram.ts`)

Это ключевой хук, который является мостом между React и клиентом Telegram.
-   **Аутентификация**: Получает данные пользователя (`user`) из `initDataUnsafe`.
-   **Управление UI**: Контролирует нативные элементы, такие как `MainButton` (главная кнопка внизу), `showAlert`, `showConfirm`.
-   **Режим разработки**: Содержит логику для мокирования данных (`isDev`), что позволяет разрабатывать и тестировать приложение в обычном браузере, имитируя окружение Telegram. `ADMIN_USER_ID` также задан в этом файле для тестирования админ-панели.

## 📝 Требования к бэкенду

Этот раздел предназначен для бэкенд-разработчика. Фронтенд полностью готов к интеграции со следующими API.

### 1. Аутентификация

-   **Метод**: Все запросы, требующие аутентификации (создание услуги, заказа и т.д.), должны содержать заголовок `Authorization`, в котором передается строка `window.Telegram.WebApp.initData`.
-   **Валидация**: На бэкенде вы **обязаны** [валидировать эту строку](https://core.telegram.org/bots/webapps#validating-data-received-via-the-web-app), чтобы убедиться, что запрос подлинный и пришел от конкретного пользователя. Это защищает ваши API от несанкционированного доступа.

### 2. Схемы базы данных

Вам понадобятся как минимум три таблицы:

1.  **`Users`** (Исполнители)
    -   `telegram_id`: `BIGINT`, `PRIMARY KEY`
    -   `first_name`, `username`: `VARCHAR`
    -   `performer_name`: `VARCHAR` (Имя, указанное в анкете)
    -   `performer_bio`: `TEXT`
    -   `performer_avatar_url`: `VARCHAR` (URL к файлу в облаке)
    -   `role`: `VARCHAR` (например, `'performer'`, `'admin'`)

2.  **`Services`** (Услуги)
    -   `id`: `UUID` или `SERIAL`, `PRIMARY KEY`
    -   `performer_id`: `BIGINT`, `FOREIGN KEY` -> `Users(telegram_id)`
    -   `title`, `description`, `image_url`
    -   `status`: `VARCHAR` (`'pending'`, `'approved'`, `'rejected'`)
    -   `created_at`, `updated_at`: `TIMESTAMP`

3.  **`Orders`** (Заказы)
    -   `id`: `UUID` или `SERIAL`, `PRIMARY KEY`
    -   `service_id`, `customer_id`, `performer_id`
    -   `details`: `TEXT` (пожелания клиента)
    -   `file_url`: `VARCHAR`, `NULLABLE`
    -   `status`: `VARCHAR` (`'new'`, `'in_progress'`, `'completed'`)
    -   `created_at`: `TIMESTAMP`

### 3. API Эндпоинты

**Публичные эндпоинты:**
-   `GET /api/services`: Получить список всех услуг со статусом `'approved'`. *Заменит текущую загрузку из localStorage.*
-   `GET /api/services/:id`: Получить детальную информацию по одной услуге.

**Эндпоинты для Исполнителей (требуют аутентификации):**
-   `POST /api/services`: Создать новую услугу. Бэкенд должен принять данные, загрузить изображения в облако (см. ниже) и создать запись в БД со статусом `'pending'`.
-   `GET /api/me/services`: Получить список всех услуг, созданных текущим пользователем.

**Эндпоинты для Заказчиков (требуют аутентификации):**
-   `POST /api/orders`: Создать новый заказ.

**Эндпоинты для Администратора (требуют аутентификации и роли 'admin'):**
-   `GET /api/admin/services?status=pending`: Получить список услуг на модерацию.
-   `PATCH /api/admin/services/:id`: Изменить статус услуги (например, `{ "status": "approved" }`).

### 4. Хранение файлов

-   Изображения (аватары, фото услуг) и файлы в заказах не могут храниться в `localStorage`. Вам необходимо настроить облачное хранилище (например, **Amazon S3, Google Cloud Storage, Cloudflare R2**).
-   **Процесс**: Фронтенд отправит файл на ваш бэкенд -> Бэкенд загрузит его в облако -> Бэкенд сохранит полученный URL в базу данных.

## 🚀 Запуск и развертывание

Приложение использует Vite и может быть легко развернуто на любой статической хостинг-платформе.

1.  **Установка зависимостей**:
    ```bash
    npm install
    ```
2.  **Локальный запуск для разработки**:
    ```bash
    npm run dev
    ```
    Приложение откроется в браузере. Благодаря мокированию данных в `useTelegram.ts`, вы можете разрабатывать и тестировать интерфейс без необходимости запускать его через Telegram.

3.  **Сборка для продакшена**:
    ```bash
    npm run build
    ```
    Эта команда создаст оптимизированную статическую версию приложения в папке `dist`.

4.  **Деплой**:
    -   Загрузите содержимое папки `dist` на любой статический хостинг (Vercel, Netlify, GitHub Pages).
    -   Получите публичный URL вашего развернутого приложения.

5.  **Интеграция с Telegram-ботом**:
    -   Перейдите в `@BotFather` в Telegram.
    -   Выберите вашего бота и зайдите в `Bot Settings` -> `Menu Button`.
    -   Установите URL вашего развернутого приложения в качестве URL для кнопки меню.

## 🔮 Планы на будущее

-   **Полноценный бэкенд**: Замена `localStorage` на реальные API-запросы.
-   **Система отзывов и рейтинга**: Возможность для клиентов оставлять отзывы и оценивать работу.
-   **Внутренний чат**: Общение между заказчиком и исполнителем.
-   **Интеграция с платежными системами**: Безопасная оплата заказов через Telegram Payments.
-   **Уведомления**: Push-уведомления для исполнителей о новых заказах через Telegram Bot API.
